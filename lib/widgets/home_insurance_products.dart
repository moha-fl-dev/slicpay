import 'package:flutter/material.dart';

class HomeInsuranceProductsWidget extends StatelessWidget {
  const HomeInsuranceProductsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hello world!"),
    );
  }
}
